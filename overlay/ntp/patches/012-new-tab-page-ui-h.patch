diff --git a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.h b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.h
index f83f42638ab95..63693e44090ed 100644
--- a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.h
+++ b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.h
@@ -32,6 +32,7 @@
 #if !defined(OFFICIAL_BUILD)
 #include "chrome/browser/ui/webui/new_tab_page/foo/foo.mojom.h"  // nogncheck crbug.com/1125897
 #endif
+#include "chrome/browser/ui/webui/new_tab_page/simprint_ip/simprint_ip.mojom.h"
 #include "base/memory/weak_ptr.h"
 #include "base/scoped_observation.h"
 #include "chrome/browser/search/background/ntp_custom_background_service_observer.h"
@@ -80,6 +81,7 @@ class DriveSuggestionHandler;
 #if !defined(OFFICIAL_BUILD)
 class FooHandler;
 #endif
+class SimprintIpHandler;
 class GoogleCalendarPageHandler;
 class OutlookCalendarPageHandler;
 class GURL;
@@ -215,6 +217,12 @@ class NewTabPageUI
       mojo::PendingReceiver<foo::mojom::FooHandler> pending_receiver);
 #endif
 
+  // Instantiates the implementor of the simprint_ip::mojom::SimprintIpHandler
+  // mojo interface passing the pending receiver that will be internally bound.
+  void BindInterface(
+      mojo::PendingReceiver<simprint_ip::mojom::SimprintIpHandler>
+          pending_receiver);
+
   void BindInterface(mojo::PendingReceiver<ntp::tab_groups::mojom::PageHandler>
                          pending_page_handler);
 
@@ -365,6 +373,7 @@ class NewTabPageUI
 #if !defined(OFFICIAL_BUILD)
   std::unique_ptr<FooHandler> foo_handler_;
 #endif
+  std::unique_ptr<SimprintIpHandler> simprint_ip_handler_;
   std::unique_ptr<MostRelevantTabResumptionPageHandler>
       most_relevant_tab_resumption_handler_;
   std::unique_ptr<page_image_service::ImageServiceHandler>
