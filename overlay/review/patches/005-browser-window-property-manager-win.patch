diff --git a/chrome/browser/ui/views/frame/browser_window_property_manager_win.cc b/chrome/browser/ui/views/frame/browser_window_property_manager_win.cc
index 1a62480aee22c..aac334643d801 100644
--- a/chrome/browser/ui/views/frame/browser_window_property_manager_win.cc
+++ b/chrome/browser/ui/views/frame/browser_window_property_manager_win.cc
@@ -49,6 +49,8 @@ void BrowserWindowPropertyManager::UpdateWindowProperties() {
   const Browser* browser = view_->browser();
   Profile* profile = browser->profile();
 
+  LOG(INFO) << "[Simprint] UpdateWindowProperties called";
+
   // Set the app user model id for this application to that of the application
   // name. See http://crbug.com/7028.
   std::wstring app_id =
@@ -58,6 +60,8 @@ void BrowserWindowPropertyManager::UpdateWindowProperties() {
                 base::UTF8ToWide(browser->app_name()), profile->GetPath())
           : shell_integration::win::GetAppUserModelIdForBrowser(
                 profile->GetPath());
+
+  LOG(INFO) << "[Simprint] App ID: " << app_id;
   // Apps set their relaunch details based on app's details.
   if (browser->is_type_app() || browser->is_type_app_popup()) {
     ExtensionRegistry* registry = ExtensionRegistry::Get(profile);
@@ -88,8 +92,10 @@ void BrowserWindowPropertyManager::UpdateWindowProperties() {
                                             &pinned_name, &icon_path);
     command_line_string = command_line.GetCommandLineString();
   }
+  LOG(INFO) << "[Simprint] Calling SetAppDetailsForWindow with app_id: " << app_id;
   ui::win::SetAppDetailsForWindow(app_id, icon_path, 0, command_line_string,
                                   pinned_name, hwnd_);
+  LOG(INFO) << "[Simprint] SetAppDetailsForWindow completed";
 }
 
 // static
