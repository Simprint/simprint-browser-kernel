diff --git a/simprint/fingerprint/fingerprint_config.cc b/simprint/fingerprint/fingerprint_config.cc
index f88dcc351c7c8..d4709646fbbb5 100644
--- a/simprint/fingerprint/fingerprint_config.cc
+++ b/simprint/fingerprint/fingerprint_config.cc
@@ -216,6 +216,12 @@ bool FingerprintConfigManager::ApplyConfigFromJson(const std::string& json_str)
   if (std::optional<bool> val = dict.FindBool("random_fingerprint_on_launch"))
     config_.random_fingerprint_on_launch = *val;
 
+  // 环境信息
+  if (const std::string* val = dict.FindString("env_id"))
+    config_.env_id = *val;
+  if (const std::string* val = dict.FindString("env_name"))
+    config_.env_name = *val;
+
   config_loaded_ = true;
 
   SIMPRINT_LOG("指纹配置解析完成");
